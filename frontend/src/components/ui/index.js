/**
 * Barrel export de componentes UI
 * @module components/ui
 *
 * Exporta todos los componentes base del sistema de diseño.
 */

export { default as Button } from './Button';
export { default as Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from './Card';
export { default as Input } from './Input';
export { default as Badge, OrderStatusBadge } from './Badge';
export { default as Spinner, LoadingOverlay, LoadingContainer } from './Spinner';
export { default as Modal, ConfirmDialog } from './Modal';
