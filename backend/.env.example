# Archivo de ejemplo para configuración del backend
# NO subir este archivo con valores reales al repositorio

# === Base de Datos ===
# Railway proporciona DATABASE_URL automáticamente
# Para desarrollo local:
DATABASE_URL=jdbc:postgresql://localhost:5432/cafe_limon_dev
DB_USERNAME=postgres
DB_PASSWORD=postgres

# === JWT Secret ===
# IMPORTANTE: Genera un secreto seguro para producción
# Mínimo 32 caracteres aleatorios
# Ejemplo para generar: openssl rand -base64 32
JWT_SECRET=CAMBIAR-POR-UN-SECRET-SEGURO-DE-AL-MENOS-32-CARACTERES

# === CORS ===
# URL del frontend (Vercel te da esta URL al desplegar)
FRONTEND_URL=https://tu-app.vercel.app

# === Configuración de la Aplicación ===
SPRING_PROFILES_ACTIVE=prod
PORT=8080

# === QR Code Base URL ===
# URL base para generar códigos QR (misma que FRONTEND_URL)
QR_BASE_URL=https://tu-app.vercel.app
